# Dependencies
bun.lockb
node_modules

# Docker
build.log

# For someone who has a broken WSL that cannot install and dependencies with Bun in both /mnt and ~/Public
yarn.lock
package-lock.json

# Other stuff
results_